import { HomeIcon } from "lucide-react"

const Sidebar = () => {

    const navItems = [
        {path: '/', label: 'Home', icon: HomeIcon}
    ]

  return (
    <div>
        Sidebar
    </div>
  )
}

export default Sidebar
